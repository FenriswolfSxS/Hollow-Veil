import { useEffect, useState } from 'react';
type Me={authenticated:boolean;user?:{username:string;avatarUrl:string;role:string;characterName?:string}};
export default function Profile(){const[me,setMe]=useState<Me>({authenticated:false}),[invite,setInvite]=useState('');useEffect(()=>{fetch('/api/me').then(r=>r.json()).then(setMe)},[]);const reveal=()=>fetch('/api/discord-invite').then(async r=>{if(r.ok)setInvite((await r.json()).url);else alert('The Discord entrance is visible only to approved Hollow Veil members.');});return <section className="page"><header className="page-heading"><p className="eyebrow">Your Place in the Veil</p><h1>Member Profile</h1></header><div className="profile-panel ornamental">{!me.authenticated?<><h2>You have not entered yet.</h2><p>Sign in with Discord to connect your identity and request member access.</p><a className="gold-button" href="/api/auth/discord">Sign in with Discord</a></>:<><img className="profile-avatar" src={me.user?.avatarUrl}/><h2>{me.user?.username}</h2><p className="role-badge">{me.user?.role}</p><p>The private Discord link remains hidden until an administrator approves this account as a member.</p>{invite?<a className="gold-button" href={invite} target="_blank" rel="noreferrer">Open Hollow Veil Discord</a>:<button className="gold-button" onClick={reveal}>Reveal member Discord</button>}</>}</div></section>}
