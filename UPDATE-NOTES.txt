HOLLOW VEIL UPDATE

Landing page
- Original artwork remains unchanged.
- Artwork is centered inside a cinematic black frame and is never cropped.
- Added subtle mist, ember particles, fade-in, and Enter transition.
- Responsive behavior for ultrawide, standard desktop, and mobile screens.

Roster page
- Automatically loads from the configured official Lodestone Free Company member URL.
- Reads every detected Lodestone roster page, not only page one.
- Removes duplicate members and caches results in D1 for 30 minutes.
- Manual Sync now button forces a fresh Lodestone request.
- Falls back to the latest cached roster if Lodestone is temporarily unavailable.
- Displays member count, last update time, search, portraits, ranks, worlds, and profile links.

No new npm dependencies were added.

JULY 30 FIXES
- Landing action now reads: “Walk not far where the shadows veil.”
- Reduced the height and padding of the home-page hero panel.
- Interior backgrounds now preserve the original artwork aspect ratio instead of enlarging it with cover.
- Rebuilt the Lodestone parser around character profile links and current member-card classes.
- The live roster no longer fails when the D1 cache table has not yet been migrated or bound.
- Cache read/write failures are isolated so a successful Lodestone response still reaches the page.

Expanded roster update:
- Added FC rank, current Lodestone class/job, job icon, and current level to member cards.
- Added search across name, rank, job, level, world, and Grand Company.
- Added rank and job filters plus rank/name/level/job sorting.
- Added responsive expanded cards for desktop and mobile.
- Updated the Lodestone parser and cached roster payload with the new fields.
